# OR-SIM frontend environment variables
# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to .env.local and fill in the values for your setup.
# Vite only exposes variables prefixed with VITE_ to the browser bundle.

# Remote backend URL (no trailing slash)
# ─────────────────────────────────────
# When running the FastAPI backend on Kaggle (or any remote host) via ngrok,
# set this to the ngrok HTTPS URL printed by the Kaggle notebook.
#
# Example:
#   VITE_BACKEND_URL=https://abcd-1234.ngrok-free.app
#
# Leave blank (or delete the line) when running the backend locally —
# the Vite dev server proxy takes care of /api and /ws automatically.
VITE_BACKEND_URL=
